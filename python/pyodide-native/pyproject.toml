[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "pyodide-native"
requires-python = ">=3.14"
version = "0.1.0"

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "pyodide_native"
